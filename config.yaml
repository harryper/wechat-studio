# WeChat Studio 配置（敏感值使用环境变量，不含明文密钥）
# 必填环境变量：
#   WECHAT_APPID       - 微信公众号 appid
#   WECHAT_SECRET      - 微信公众号 secret
#   OPENAI_API_KEY     - OpenAI API Key（用于 gpt-image-2）
#   MINIMAX_API_KEY    - MiniMax API Key（文章生成 + 图片生成）
# 可选：
#   DASHSCOPE_API_KEY  - 阿里通义万相
#   JIMENG_ACCESS_KEY  - 即梦 access_key_id
#   JIMENG_SECRET_KEY  - 即梦 secret_access_key

wechat:
  appid: "${WECHAT_APPID}"
  secret: "${WECHAT_SECRET}"
  author: ""  # 默认署名（可选）

# AI 图片生成（多 provider 自动 fallback）
image:
  providers:
    - provider: "minimax"
      api_key: "${MINIMAX_API_KEY}"
      model: "image-01"
      base_url: "https://api.minimaxi.com/v1"
    - provider: "openai"
      # 默认关闭，避免产生 OpenAI 费用；需要时显式 export OPENAI_IMAGE_ENABLED=true
      enabled: "${OPENAI_IMAGE_ENABLED:-false}"
      api_key: "${OPENAI_API_KEY}"
      model: "gpt-image-2"
      base_url: "https://api.openai.com/v1"

# 默认排版主题
theme: "terracotta"
